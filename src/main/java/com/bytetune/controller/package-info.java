/**
 * 控制器包
 * <p>
 * 作用：
 * - 提供对外 HTTP 接口
 * - 接收请求、调用服务层处理、返回响应
 * <p>
 * 常见内容：
 * - RestController / Controller 类
 * - 接口分组：SongController、UserController 等
 * - API 请求校验注解
 */
package com.bytetune.controller;