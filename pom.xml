<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!-- ====================================================== -->
    <!-- 项目基本信息 -->
    <!-- ====================================================== -->
    <groupId>com.maizi</groupId>
    <artifactId>bytetune</artifactId>
    <version>0.0.1-SNAPSHOT</version>

    <packaging>pom</packaging>

    <name>bytetune</name>
    <description>ByteTune 音乐管理系统父项目</description>

    <!-- ====================================================== -->
    <!-- 子模块 -->
    <!-- ====================================================== -->
    <modules>
        <!-- 认证服务 -->
        <module>auth-service</module>
        <!-- 音乐核心服务 -->
        <module>bytetune-song-service</module>
        <!-- 歌词服务 -->
        <module>bytetune-lyric-service</module>
        <!-- 公共基础模块 -->
        <module>bytetune-common</module>
        <!-- 文件服务 -->
        <module>bytetune-file-service</module>
        <!-- MinIO 基础组件 -->
        <module>bytetune-storage-service</module>
        <!-- 监控与日志 -->
        <module>bytetune-observe</module>
        <!-- 消息传递/中间件 -->
        <module>bytetune-messaging</module>
        <!-- 跨服务通信契约 -->
        <module>bytetune-contract</module>
    </modules>

    <!-- ======================= 全局属性 ======================= -->
    <properties>
        <java.version>17</java.version>
        <spring.boot.version>3.3.5</spring.boot.version>
        <mybatis.plus.version>3.5.15</mybatis.plus.version>
        <minio.version>8.5.10</minio.version>
        <tika.version>2.9.0</tika.version>
        <druid.version>1.2.27</druid.version>
        <lombok.version>1.18.30</lombok.version>
        <springdoc.version>2.5.0</springdoc.version>
        <mongodb.version>3.3.5</mongodb.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencies>
        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <!-- ======================= 依赖管理 ======================= -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.maizi</groupId>
                <artifactId>auth-service</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- ByteTune 公共模块 -->
            <dependency>
                <groupId>com.maizi</groupId>
                <artifactId>bytetune-common</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>com.maizi</groupId>
                <artifactId>bytetune-file-service</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.maizi</groupId>
                <artifactId>bytetune-storage-service</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.maizi</groupId>
                <artifactId>bytetune-observe</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>com.maizi</groupId>
                <artifactId>bytetune-messaging</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>com.maizi</groupId>
                <artifactId>bytetune-contract</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Spring Boot 统一依赖版本 -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- MyBatis-Plus -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis.plus.version}</version>
            </dependency>
            <!-- MinIO -->
            <dependency>
                <groupId>io.minio</groupId>
                <artifactId>minio</artifactId>
                <version>${minio.version}</version>
            </dependency>
            <!-- Apache Tika -->
            <dependency>
                <groupId>org.apache.tika</groupId>
                <artifactId>tika-core</artifactId>
                <version>${tika.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.tika</groupId>
                <artifactId>tika-parsers-standard-package</artifactId>
                <version>${tika.version}</version>
            </dependency>
            <!-- Druid -->
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>druid-spring-boot-3-starter</artifactId>
                <version>${druid.version}</version>
            </dependency>

            <!-- Springdoc OpenAPI -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <!-- mongodb-->
            <!-- Spring Boot starter 不建议直接指定版本（最好让 Spring Boot BOM 管理），否则可能和 Spring Boot 版本不一致。-->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-data-mongodb</artifactId>
                <version>${mongodb.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring.boot.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <release>${java.version}</release>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>